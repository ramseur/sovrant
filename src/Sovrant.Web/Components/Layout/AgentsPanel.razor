@*  Agents group secondary panel — templates and orchestration.
    Command Center lives under Admin since it surfaces every user's runs. *@

<div class="rail-secondary-header">Agents</div>
<NavButton Href="/agents" Label="Library" OnClick="OnNavigate" />
<NavButton Href="/orchestration" Label="Orchestration" OnClick="OnNavigate" />
<NavButton Href="/workflows" Label="Workflows" OnClick="OnNavigate" />

@code {
    [Parameter] public EventCallback<string> OnNavigate { get; set; }
}
